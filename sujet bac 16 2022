def maxi(tab):
    res = 0
    i_max = 0
    for i in range(len(tab)):
        if tab[i] > res :
            res = tab[i]
            i_max = i
    return res,i_max
print(maxi([1,5,6,9,1,2,3,7,9,8]))
